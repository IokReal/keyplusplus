<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".Login">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    <TextView
        android:visibility="visible"
        android:id="@+id/textView5"
        android:layout_width="match_parent"
        android:layout_height="@android:dimen/thumbnail_height"
        android:background="@android:color/background_dark"
        android:gravity="center"
        android:text="Для получения ключа необходимо один раз открыть дверь через стандартный интерфейс\n!!!Откажитесь от получения уведомлений!!!"
        android:textColor="@android:color/white"
        app:layout_anchor="@+id/webView"
        app:layout_anchorGravity="center">

    </TextView>

    <Button
        android:visibility="visible"
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Подождите с"
        android:enabled="false"
        android:onClick="close"
        app:layout_anchor="@+id/textView5"
        app:layout_anchorGravity="bottom|center"/>
    <Button
        android:visibility="gone"
        android:id="@+id/button2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Выйти"
        android:onClick="exit"
        app:layout_anchor="@+id/textView5"
        app:layout_anchorGravity="bottom|center" />




</androidx.coordinatorlayout.widget.CoordinatorLayout>